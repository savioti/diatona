/// Which chord tone the arpeggio starts on.
///
/// [random] puts every inversion of every chord in the pool, including the
/// third inversion of seventh chords, which has no chip of its own.
enum ArpeggioInversion { root, first, second, random }
